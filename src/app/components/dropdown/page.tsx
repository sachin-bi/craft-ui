import { ProfileDropdown } from "@/components/ui/profile-dropdown";

export default function DropdownPage() {
    return (
        <div className="min-h-screen bg-slate-50 p-10 dark:bg-gray-900 flex justify-center">
            <ProfileDropdown />
        </div>
    );
}
